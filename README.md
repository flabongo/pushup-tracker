# Ladder — Pushup Tracker

A daily pushup ladder tracker: the number of pushups grows by one rung each day of the year, split into manageable sets.

## Development

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

Deploys automatically to GitHub Pages via GitHub Actions on every push to `main`.
